#![feature(unboxed_closures)]
#![feature(fn_traits)]
#![allow(unused)]

mod json_get_macros;
mod tasks;
mod tree_hash;

fn main() {
    println!("Just run the tests (cargo test) to see different tasks exections!");
}
